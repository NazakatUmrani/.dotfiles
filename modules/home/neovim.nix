{ config, ... }:

{
  # Commented out due to switching to NVF

  # programs.neovim = {
  #   enable = true;
  #   defaultEditor = true;
  #   viAlias = true;
  #   vimAlias = true;
  #   vimdiffAlias = true;
  #   extraPackages = with pkgs; [ xclip wl-clipboard ];
  # };

  # xdg = {
  #   configFile."nvim" = {
  #     source = ../../configs/nvim;
  #     recursive = true;
  #   };
  # };
}
